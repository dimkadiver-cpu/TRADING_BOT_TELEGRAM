# TeleSignalBot — channels configuration
# Il listener rilegge questo file senza restart (hot reload via watchdog).

recovery:
  max_hours: 4  # finestra temporale messaggi recuperati al restart

blacklist_global:
  - "#admin"
  - "#админ"
  - "#info"
  - "#pinned"
  - "Админ на связи"

channels:
  - chat_id: -1003171748254
    label: "PifSignal"
    active: true
    trader_id: null   # canale multi-trader — risoluzione via alias/reply-chain
    blacklist: []
