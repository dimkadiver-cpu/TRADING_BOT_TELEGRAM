# Collisioni weak → strong

Fonte: `semantic_markers.json`.

Definizione usata: una collisione esiste quando un marker `weak` appare come sottostringa dentro un marker `strong` di un altro gruppo.

Totale: **16 gruppi sorgente**, **78 weak marker coinvolti**, **180 blocchi target**, **1178 strong marker colpiti**.

---

## Indice sintetico

- `intent_markers.MOVE_STOP_TO_BE`: `в бу`, `безубыток`, `на вход` — 60 match
- `intent_markers.MOVE_STOP`: `стоп на` — 7 match
- `intent_markers.CLOSE_FULL`: `зафиксируем`, `зафиксировать`, `закрыта`, `закрываю`, `закрылся` — 51 match
- `intent_markers.CANCEL_PENDING`: `отбой` — 9 match
- `intent_markers.MODIFY_ENTRY`: `с текущих`, `по текущим`, `тут с текущих`, `по рынку`, `вход`, `переносим`, `меняем`, `убираем` — 275 match
- `intent_markers.ADD_ENTRY`: `добавляю`, `добавляем`, `добавить`, `добавлю`, `добавим`, `добор`, `усреднение` — 54 match
- `intent_markers.ENTRY_FILLED`: `сработал`, `взяли`, `лимитка`, `добор`, `усреднение` — 67 match
- `intent_markers.TP_HIT`: `tp1`, `tp 1`, `tp2`, `tp 2`, `tp3`, `tp 3`, `tp4`, `tp 4`, `тп1`, `тп 1`, `тп2`, `тп 2`, `тп3`, `тп 3`, `тп4`, `тп 4`, `1 тейк`, `2 тейк`, `3 тейк`, `4 тейк`, `тейк 1`, `тейк 2`, `тейк 3`, `тейк 4`, `первый тейк`, `второй тейк`, `третий тейк`, `тейк`, `тейки`, `цель`, `цели`, `таргет`, `таргеты` — 271 match
- `intent_markers.SL_HIT`: `стоп`, `sl`, `выбило` — 219 match
- `intent_markers.EXIT_BE`: `бу`, `б/у`, `безубыток`, `безубытке`, `в ноль` — 87 match
- `modify_entry_mode_markers.MARKET_NOW`: `по рынку` — 26 match
- `entry_type_markers.LIMIT`: `лимиткой` — 1 match
- `target_hint_markers.ALL_LONG`: `лонги` — 7 match
- `target_hint_markers.ALL_SHORT`: `шорты`, `short` — 8 match
- `field_markers.symbol`: `.p` — 1 match
- `field_markers.entry`: `усреднение` — 35 match

---

## `intent_markers.MOVE_STOP_TO_BE`

### Weak `в бу` in 1 target group(s), 33 strong marker(s)

`intent_markers.EXIT_BE` strong:

```json
[
  "ушел в бу",
  "ушёл в бу",
  "ушла в бу",
  "ушло в бу",
  "позиция ушла в бу",
  "сделка ушла в бу",
  "остаток ушел в бу",
  "остаток ушёл в бу",
  "остаток позиции ушел в бу",
  "остаток позиции ушёл в бу",
  "остаток закрылся в бу",
  "остаток закрыт в бу",
  "сделка закрылась в бу",
  "сделка закрыта в бу",
  "закрылся в бу",
  "закрылась в бу",
  "закрылось в бу",
  "закрылись в бу",
  "закрыта в бу",
  "закрыто в бу",
  "закрыт в бу",
  "закрыты в бу",
  "в бу закрылся",
  "в бу закрылась",
  "в бу закрылось",
  "в бу закрылись",
  "также в бу закрылись",
  "тоже в бу закрылись",
  "также закрылись в бу",
  "тоже закрылись в бу",
  "стоп в бу сработал",
  "сработал стоп в бу",
  "выбило в бу"
]
```

### Weak `безубыток` in 1 target group(s), 23 strong marker(s)

`intent_markers.EXIT_BE` strong:

```json
[
  "позиция ушла в безубыток",
  "сделка ушла в безубыток",
  "остаток ушел в безубыток",
  "остаток позиции ушел в безубыток",
  "остаток закрылся в безубыток",
  "остаток закрыт в безубыток",
  "сделка закрылась в безубыток",
  "сделка закрыта в безубыток",
  "закрылся в безубыток",
  "закрылась в безубыток",
  "закрылось в безубыток",
  "закрылись в безубыток",
  "закрыта в безубыток",
  "закрыто в безубыток",
  "закрыт в безубыток",
  "закрыты в безубыток",
  "в безубыток закрылся",
  "в безубыток закрылась",
  "в безубыток закрылось",
  "в безубыток закрылись",
  "также закрылись в безубыток",
  "тоже закрылись в безубыток",
  "выбило в безубыток"
]
```

### Weak `на вход` in 1 target group(s), 4 strong marker(s)

`intent_markers.MODIFY_ENTRY` strong:

```json
[
  "новая цена входа",
  "новая зона входа",
  "лимитку на вход переносим",
  "переносим лимитку на вход"
]
```

---

## `intent_markers.MOVE_STOP`

### Weak `стоп на` in 1 target group(s), 7 strong marker(s)

`intent_markers.MOVE_STOP_TO_BE` strong:

```json
[
  "стоп на точку входа",
  "поставить стоп на точку входа",
  "поставим стоп на точку входа",
  "ставим стоп на точку входа",
  "стоп на вход",
  "стоп на цену входа",
  "стоп на ноль"
]
```

---

## `intent_markers.CLOSE_FULL`

### Weak `зафиксируем` in 1 target group(s), 2 strong marker(s)

`intent_markers.CLOSE_PARTIAL` strong:

```json
[
  "зафиксируем 50%",
  "частично зафиксируем"
]
```

### Weak `зафиксировать` in 4 target group(s), 9 strong marker(s)

`intent_markers.CLOSE_PARTIAL` strong:

```json
[
  "зафиксировать 50%",
  "зафиксировать часть",
  "частично зафиксировать"
]
```

`target_hint_markers.ALL_LONG` strong:

```json
[
  "зафиксировать лонги",
  "зафиксировать все лонги"
]
```

`target_hint_markers.ALL_SHORT` strong:

```json
[
  "зафиксировать шорты",
  "зафиксировать все шорты"
]
```

`target_hint_markers.ALL_POSITIONS` strong:

```json
[
  "зафиксировать все",
  "зафиксировать все позиции"
]
```

### Weak `закрыта` in 1 target group(s), 10 strong marker(s)

`intent_markers.EXIT_BE` strong:

```json
[
  "сделка закрыта в бу",
  "сделка закрыта в б/у",
  "сделка закрыта в безубыток",
  "сделка закрыта по бу",
  "сделка закрыта по б/у",
  "закрыта в бу",
  "закрыта в б/у",
  "закрыта в безубыток",
  "закрыта по бу",
  "закрыта по б/у"
]
```

### Weak `закрываю` in 4 target group(s), 17 strong marker(s)

`intent_markers.CLOSE_PARTIAL` strong:

```json
[
  "закрываю 25%",
  "закрываю 30%",
  "закрываю 50%",
  "закрываю 70%",
  "закрываю 80%",
  "частично закрываю",
  "закрываю часть",
  "половину закрываю",
  "закрываю половину"
]
```

`target_hint_markers.ALL_LONG` strong:

```json
[
  "закрываю лонги",
  "закрываю все лонги"
]
```

`target_hint_markers.ALL_SHORT` strong:

```json
[
  "закрываю шорты",
  "закрываю все шорты"
]
```

`target_hint_markers.ALL_POSITIONS` strong:

```json
[
  "закрываю все",
  "закрываю все позиции",
  "закрываю все по текущим",
  "закрываю все по рынку"
]
```

### Weak `закрылся` in 2 target group(s), 13 strong marker(s)

`intent_markers.SL_HIT` strong:

```json
[
  "закрылся по стопу"
]
```

`intent_markers.EXIT_BE` strong:

```json
[
  "остаток закрылся в бу",
  "остаток закрылся в безубыток",
  "закрылся в бу",
  "закрылся в б/у",
  "закрылся в безубыток",
  "закрылся по бу",
  "закрылся по б/у",
  "в безубыток закрылся",
  "в бу закрылся",
  "в б/у закрылся",
  "без убытка закрылся",
  "закрылся в ноль"
]
```

---

## `intent_markers.CANCEL_PENDING`

### Weak `отбой` in 1 target group(s), 9 strong marker(s)

`intent_markers.INVALIDATE_SETUP` strong:

```json
[
  "тоже отбой",
  "тут отбой",
  "здесь отбой",
  "по этому отбой",
  "по этой отбой",
  "отбой по сигналу",
  "отбой по сделке",
  "отбой по сетапу",
  "отбой по идее"
]
```

---

## `intent_markers.MODIFY_ENTRY`

### Weak `с текущих` in 4 target group(s), 32 strong marker(s)

`intent_markers.ADD_ENTRY` strong:

```json
[
  "добавляю с текущих",
  "добавляем с текущих",
  "добираю с текущих",
  "добираем с текущих"
]
```

`modify_entry_mode_markers.MARKET_NOW` strong:

```json
[
  "вход с текущих",
  "входим с текущих",
  "заходим с текущих",
  "зайду с текущих",
  "зайдем с текущих",
  "зайдём с текущих",
  "беру с текущих",
  "берем с текущих",
  "берём с текущих",
  "открываю с текущих",
  "открываем с текущих",
  "тут уже вход с текущих",
  "тут вход с текущих",
  "тут с текущих",
  "попробуем шорт с текущих",
  "попробуем лонг с текущих",
  "пробуем шорт с текущих",
  "пробуем лонг с текущих",
  "шорт с текущих",
  "лонг с текущих",
  "шортим с текущих",
  "лонгуем с текущих",
  "с текущих без лимитки"
]
```

`entry_type_markers.MARKET` strong:

```json
[
  "вход с текущих",
  "a (с текущих)"
]
```

`field_markers.entry` strong:

```json
[
  "вход с текущих:",
  "(вход с текущих)",
  "a (с текущих)"
]
```

### Weak `по текущим` in 6 target group(s), 37 strong marker(s)

`intent_markers.CLOSE_FULL` strong:

```json
[
  "закрываю по текущим",
  "закрою по текущим",
  "закрыть по текущим",
  "закрыл по текущим",
  "закрываю все по текущим",
  "закрываю остаток по текущим",
  "фиксирую по текущим",
  "зафиксирую по текущим",
  "фиксация по текущим",
  "фиксация 100% по текущим отметкам",
  "закрыть лонги по текущим",
  "закрыть шорты по текущим",
  "зафиксирую все свои позиции по текущим"
]
```

`intent_markers.ADD_ENTRY` strong:

```json
[
  "добавляю по текущим",
  "добавляем по текущим",
  "добираю по текущим",
  "добираем по текущим"
]
```

`intent_markers.REENTER` strong:

```json
[
  "заходим по текущим заново"
]
```

`modify_entry_mode_markers.MARKET_NOW` strong:

```json
[
  "входим по текущим",
  "вход по текущим",
  "заходим по текущим",
  "зайду по текущим",
  "зайдем по текущим",
  "зайдём по текущим",
  "беру по текущим",
  "берем по текущим",
  "берём по текущим",
  "открываю по текущим",
  "открываем по текущим",
  "открыть по текущим",
  "шортим по текущим",
  "лонгуем по текущим"
]
```

`target_hint_markers.ALL_LONG` strong:

```json
[
  "закрыть лонги по текущим"
]
```

`target_hint_markers.ALL_POSITIONS` strong:

```json
[
  "закрываю все по текущим",
  "закрываем все по текущим",
  "закрыть все по текущим",
  "закрою все по текущим"
]
```

### Weak `тут с текущих` in 1 target group(s), 1 strong marker(s)

`modify_entry_mode_markers.MARKET_NOW` strong:

```json
[
  "тут с текущих"
]
```

### Weak `по рынку` in 4 target group(s), 24 strong marker(s)

`intent_markers.CLOSE_FULL` strong:

```json
[
  "закрываю по рынку",
  "закрываем по рынку",
  "закрыть по рынку",
  "закрою по рынку",
  "закрываю все по рынку",
  "закрытие по рынку",
  "выход по рынку"
]
```

`modify_entry_mode_markers.MARKET_NOW` strong:

```json
[
  "входим по рынку",
  "вход по рынку",
  "заходим по рынку",
  "зайду по рынку",
  "зайдем по рынку",
  "зайдём по рынку",
  "беру по рынку",
  "берем по рынку",
  "берём по рынку",
  "открываю по рынку",
  "открываем по рынку",
  "открыть по рынку"
]
```

`target_hint_markers.ALL_LONG` strong:

```json
[
  "закрыть лонги по рынку"
]
```

`target_hint_markers.ALL_POSITIONS` strong:

```json
[
  "закрываю все по рынку",
  "закрываем все по рынку",
  "закрыть все по рынку",
  "закрою все по рынку"
]
```

### Weak `вход` in 15 target group(s), 115 strong marker(s)

`intent_markers.MOVE_STOP_TO_BE` strong:

```json
[
  "стоп поставим на точку входа",
  "стопы поставим на точку входа",
  "стоп на точку входа",
  "стопы на точку входа",
  "стоп обязательно на точку входа",
  "стопы обязательно на точку входа",
  "поставить стоп на точку входа",
  "поставить стопы на точку входа",
  "поставить на точку входа",
  "поставим стоп на точку входа",
  "поставим стопы на точку входа",
  "ставим стоп на точку входа",
  "ставим стопы на точку входа",
  "стоп на вход",
  "стопы на вход",
  "стоп на цену входа",
  "стопы на цену входа",
  "стоп к цене входа",
  "стопы к цене входа",
  "стоп переносим на вход",
  "стопы переносим на вход",
  "стоп переносим на цену входа",
  "стопы переносим на цену входа"
]
```

`intent_markers.INVALIDATE_SETUP` strong:

```json
[
  "вход отменяется",
  "вход отменен",
  "вход отменён",
  "вход не актуален",
  "вход неактуален",
  "не входить",
  "вход пропускаем"
]
```

`intent_markers.ADD_ENTRY` strong:

```json
[
  "добавляю вход",
  "добавляем вход",
  "добавить вход",
  "добавлю вход",
  "добавим вход",
  "добавьте вход",
  "добавляю еще один вход",
  "добавляю ещё один вход",
  "добавляем еще один вход",
  "добавляем ещё один вход",
  "добавить еще один вход",
  "добавить ещё один вход",
  "добавим еще вход",
  "добавим ещё вход",
  "дополнительный вход",
  "добавочный вход",
  "еще один вход",
  "ещё один вход",
  "новый дополнительный вход",
  "добавляем дополнительный вход",
  "добавить дополнительный вход",
  "добавляю вход на",
  "добавляем вход на",
  "добавить вход на",
  "добавлю вход на",
  "добавим вход на",
  "добавляю второй вход",
  "добавляем второй вход",
  "добавить второй вход",
  "добавляю третий вход",
  "добавляем третий вход",
  "добавить третий вход",
  "второй вход добавляем",
  "третий вход добавляем"
]
```

`intent_markers.ENTRY_FILLED` strong:

```json
[
  "вход исполнен",
  "вход исполнился",
  "вход выполнен",
  "вход отработал",
  "вход взяли",
  "вход забрали",
  "средняя входа",
  "первый вход исполнен",
  "второй вход исполнен",
  "третий вход исполнен"
]
```

`modify_entry_mode_markers.MARKET_NOW` strong:

```json
[
  "входим по рынку",
  "вход по рынку",
  "входим по текущим",
  "вход по текущим",
  "вход с текущих",
  "входим с текущих",
  "тут уже вход с текущих",
  "тут вход с текущих",
  "без лимитки входим",
  "вход без лимитки"
]
```

`modify_entry_mode_markers.UPDATE_PRICE` strong:

```json
[
  "новый вход",
  "новая точка входа",
  "вход теперь",
  "вход меняем",
  "меняем вход",
  "переносим вход",
  "вход переносим",
  "лимитку входа переносим",
  "основной вход переносим"
]
```

`modify_entry_mode_markers.UPDATE_RANGE` strong:

```json
[
  "диапазон входа",
  "вход в диапазон"
]
```

`modify_entry_mode_markers.REPLACE_ENTRY` strong:

```json
[
  "заменяем вход",
  "полностью меняем вход"
]
```

`modify_entry_mode_markers.REMOVE` strong:

```json
[
  "убираем вход"
]
```

`entry_type_markers.MARKET` strong:

```json
[
  "вход с текущих"
]
```

`entry_type_markers.LIMIT` strong:

```json
[
  "вход лимиткой",
  "вход лимитом",
  "вход лимитным ордером"
]
```

`entry_selector_markers.PRIMARY` strong:

```json
[
  "основной вход",
  "первый вход",
  "вход a"
]
```

`entry_selector_markers.AVERAGING` strong:

```json
[
  "вход b"
]
```

`field_markers.entry` strong:

```json
[
  "вход с текущих:",
  "(вход с текущих)",
  "вход:",
  "вход (a)",
  "вход (b)",
  "вход (2-фазный)",
  "вход лимиткой",
  "вход лимитным ордером"
]
```

`field_markers.risk` strong:

```json
[
  "вход не более"
]
```

### Weak `переносим` in 3 target group(s), 14 strong marker(s)

`intent_markers.MOVE_STOP_TO_BE` strong:

```json
[
  "стоп переносим на вход",
  "стопы переносим на вход",
  "стоп переносим на цену входа",
  "стопы переносим на цену входа"
]
```

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп переносим на первый тейк",
  "стоп переносим на второй тейк",
  "переносим стоп",
  "стоп переносим",
  "стоп переносим на"
]
```

`modify_entry_mode_markers.UPDATE_PRICE` strong:

```json
[
  "переносим вход",
  "вход переносим",
  "лимитку входа переносим",
  "основной вход переносим",
  "усреднение переносим"
]
```

### Weak `меняем` in 5 target group(s), 26 strong marker(s)

`intent_markers.CANCEL_PENDING` strong:

```json
[
  "отменяем лимитки",
  "отменяем лимитку",
  "отменяем лимитный ордер",
  "отменяем лимитные ордера",
  "отменяем все лимитные ордера",
  "отменяем все лимитки",
  "лимитку отменяем",
  "лимитки отменяем",
  "отменяем лимитку на усреднение",
  "отменяем доборную лимитку",
  "отменяем ордер на добор"
]
```

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "меняем тейки",
  "тейки меняем",
  "меняем цели",
  "цели меняем",
  "заменяем тейки",
  "тейки заменяем",
  "заменяем цели",
  "отменяем тейк",
  "отменяем тейки",
  "меняем цель"
]
```

`modify_entry_mode_markers.UPDATE_PRICE` strong:

```json
[
  "вход меняем",
  "меняем вход"
]
```

`modify_entry_mode_markers.REPLACE_ENTRY` strong:

```json
[
  "заменяем вход",
  "полностью меняем вход"
]
```

`target_hint_markers.ALL_POSITIONS` strong:

```json
[
  "отменяем все"
]
```

### Weak `убираем` in 4 target group(s), 26 strong marker(s)

`intent_markers.CANCEL_PENDING` strong:

```json
[
  "убираем лимитки",
  "убираем лимитку",
  "убираем лимтку",
  "убираем все лимитные ордера",
  "убираем все лимитки",
  "лимитку убираем",
  "лимитки убираем",
  "лимитку на усреднение убираем",
  "лимитку на усреднение обязательно убираем",
  "лимитки на усреднение убираем",
  "убираем лимитку на усреднение",
  "убираем доборную лимитку",
  "убираем ордер на добор"
]
```

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "убираем 2 и 3 тейк",
  "убираем второй и третий тейк",
  "первый тейк убираем",
  "второй тейк убираем",
  "третий тейк убираем",
  "тейк убираем",
  "тейки убираем",
  "убираем тейк",
  "убираем тейки",
  "убираем tp",
  "убираем цель"
]
```

`modify_entry_mode_markers.REMOVE` strong:

```json
[
  "убираем вход"
]
```

`target_hint_markers.ALL_POSITIONS` strong:

```json
[
  "убираем все"
]
```

---

## `intent_markers.ADD_ENTRY`

### Weak `добавляю` in 1 target group(s), 1 strong marker(s)

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "добавляю тейк"
]
```

### Weak `добавляем` in 1 target group(s), 5 strong marker(s)

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "добавляем тейк",
  "добавляем еще один тейк",
  "добавляем ещё один тейк",
  "добавляем tp",
  "добавляем цель"
]
```

### Weak `добавить` in 1 target group(s), 5 strong marker(s)

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "добавить тейк",
  "добавить еще тейк",
  "добавить ещё тейк",
  "добавить tp",
  "добавить цель"
]
```

### Weak `добавлю` in 1 target group(s), 3 strong marker(s)

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "добавлю тейк спота",
  "добавлю тейк",
  "добавлю цель"
]
```

### Weak `добавим` in 1 target group(s), 1 strong marker(s)

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "добавим тейк"
]
```

### Weak `добор` in 2 target group(s), 14 strong marker(s)

`intent_markers.CANCEL_PENDING` strong:

```json
[
  "убираем доборную лимитку",
  "убрать доборную лимитку",
  "отменяем доборную лимитку",
  "отменить доборную лимитку",
  "снимаем доборную лимитку",
  "снять доборную лимитку",
  "убираем ордер на добор",
  "отменяем ордер на добор",
  "снимаем ордер на добор"
]
```

`intent_markers.ENTRY_FILLED` strong:

```json
[
  "добор взят",
  "взяли добор",
  "добор исполнился",
  "добор исполнен",
  "добор сработал"
]
```

### Weak `усреднение` in 6 target group(s), 25 strong marker(s)

`intent_markers.CANCEL_PENDING` strong:

```json
[
  "лимитку на усреднение убираем",
  "лимитку на усреднение обязательно убираем",
  "лимитку на усреднение можно убрать",
  "лимитки на усреднение убираем",
  "убираем лимитку на усреднение",
  "убрать лимитку на усреднение",
  "отменяем лимитку на усреднение",
  "отменить лимитку на усреднение",
  "снимаем лимитку на усреднение",
  "снять лимитку на усреднение"
]
```

`intent_markers.MODIFY_ENTRY` strong:

```json
[
  "усреднение переносим",
  "переносим усреднение",
  "усреднение меняем",
  "меняем усреднение",
  "лимитку на усреднение переносим",
  "переносим лимитку на усреднение"
]
```

`intent_markers.ENTRY_FILLED` strong:

```json
[
  "усреднение взято",
  "взяли усреднение",
  "усреднение исполнилось",
  "усреднение исполнено",
  "усреднение сработало"
]
```

`modify_entry_mode_markers.UPDATE_PRICE` strong:

```json
[
  "усреднение переносим"
]
```

`entry_selector_markers.AVERAGING` strong:

```json
[
  "усреднение",
  "лимитка на усреднение"
]
```

`field_markers.entry` strong:

```json
[
  "b (усреднение)"
]
```

---

## `intent_markers.ENTRY_FILLED`

### Weak `сработал` in 2 target group(s), 16 strong marker(s)

`intent_markers.SL_HIT` strong:

```json
[
  "стоп сработал",
  "сработал стоп",
  "стоплосс сработал",
  "stop loss сработал",
  "sl сработал",
  "сл сработал",
  "сработал стоплосс",
  "сработал stop loss",
  "сработал sl",
  "сработал сл"
]
```

`intent_markers.EXIT_BE` strong:

```json
[
  "стоп в бу сработал",
  "стоп в б/у сработал",
  "стоп в безубытке сработал",
  "сработал стоп в бу",
  "сработал стоп в б/у",
  "сработал стоп в безубытке"
]
```

### Weak `взяли` in 1 target group(s), 2 strong marker(s)

`intent_markers.TP_HIT` strong:

```json
[
  "тейк взяли",
  "взяли тейк"
]
```

### Weak `лимитка` in 1 target group(s), 1 strong marker(s)

`entry_selector_markers.AVERAGING` strong:

```json
[
  "лимитка на усреднение"
]
```

### Weak `добор` in 2 target group(s), 17 strong marker(s)

`intent_markers.CANCEL_PENDING` strong:

```json
[
  "убираем доборную лимитку",
  "убрать доборную лимитку",
  "отменяем доборную лимитку",
  "отменить доборную лимитку",
  "снимаем доборную лимитку",
  "снять доборную лимитку",
  "убираем ордер на добор",
  "отменяем ордер на добор",
  "снимаем ордер на добор"
]
```

`intent_markers.ADD_ENTRY` strong:

```json
[
  "добавляю добор",
  "добавляем добор",
  "добавить добор",
  "добавлю добор",
  "добавим добор",
  "ставим добор",
  "поставим добор",
  "поставить добор"
]
```

### Weak `усреднение` in 6 target group(s), 31 strong marker(s)

`intent_markers.CANCEL_PENDING` strong:

```json
[
  "лимитку на усреднение убираем",
  "лимитку на усреднение обязательно убираем",
  "лимитку на усреднение можно убрать",
  "лимитки на усреднение убираем",
  "убираем лимитку на усреднение",
  "убрать лимитку на усреднение",
  "отменяем лимитку на усреднение",
  "отменить лимитку на усреднение",
  "снимаем лимитку на усреднение",
  "снять лимитку на усреднение"
]
```

`intent_markers.MODIFY_ENTRY` strong:

```json
[
  "усреднение переносим",
  "переносим усреднение",
  "усреднение меняем",
  "меняем усреднение",
  "лимитку на усреднение переносим",
  "переносим лимитку на усреднение"
]
```

`intent_markers.ADD_ENTRY` strong:

```json
[
  "добавляю усреднение",
  "добавляем усреднение",
  "добавить усреднение",
  "добавлю усреднение",
  "добавим усреднение",
  "ставим усреднение",
  "поставим усреднение",
  "поставить усреднение",
  "добавляю усреднение на",
  "добавляем усреднение на",
  "добавить усреднение на"
]
```

`modify_entry_mode_markers.UPDATE_PRICE` strong:

```json
[
  "усреднение переносим"
]
```

`entry_selector_markers.AVERAGING` strong:

```json
[
  "усреднение",
  "лимитка на усреднение"
]
```

`field_markers.entry` strong:

```json
[
  "b (усреднение)"
]
```

---

## `intent_markers.TP_HIT`

### Weak `tp1` in 2 target group(s), 4 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на tp1",
  "стоп к tp1"
]
```

`field_markers.take_profit` strong:

```json
[
  "tp1:",
  "TP1:"
]
```

### Weak `tp 1` in 2 target group(s), 3 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на tp 1"
]
```

`field_markers.take_profit` strong:

```json
[
  "tp 1:",
  "TP 1:"
]
```

### Weak `tp2` in 2 target group(s), 4 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на tp2",
  "стоп к tp2"
]
```

`field_markers.take_profit` strong:

```json
[
  "tp2:",
  "TP2:"
]
```

### Weak `tp 2` in 2 target group(s), 3 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на tp 2"
]
```

`field_markers.take_profit` strong:

```json
[
  "tp 2:",
  "TP 2:"
]
```

### Weak `tp3` in 2 target group(s), 3 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на tp3"
]
```

`field_markers.take_profit` strong:

```json
[
  "tp3:",
  "TP3:"
]
```

### Weak `tp 3` in 1 target group(s), 2 strong marker(s)

`field_markers.take_profit` strong:

```json
[
  "tp 3:",
  "TP 3:"
]
```

### Weak `tp4` in 1 target group(s), 2 strong marker(s)

`field_markers.take_profit` strong:

```json
[
  "tp4:",
  "TP4:"
]
```

### Weak `tp 4` in 1 target group(s), 2 strong marker(s)

`field_markers.take_profit` strong:

```json
[
  "tp 4:",
  "TP 4:"
]
```

### Weak `тп1` in 2 target group(s), 3 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на тп1",
  "стоп к тп1"
]
```

`field_markers.take_profit` strong:

```json
[
  "тп1:"
]
```

### Weak `тп 1` in 2 target group(s), 2 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на тп 1"
]
```

`field_markers.take_profit` strong:

```json
[
  "тп 1:"
]
```

### Weak `тп2` in 2 target group(s), 3 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на тп2",
  "стоп к тп2"
]
```

`field_markers.take_profit` strong:

```json
[
  "тп2:"
]
```

### Weak `тп 2` in 2 target group(s), 2 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на тп 2"
]
```

`field_markers.take_profit` strong:

```json
[
  "тп 2:"
]
```

### Weak `тп3` in 2 target group(s), 2 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на тп3"
]
```

`field_markers.take_profit` strong:

```json
[
  "тп3:"
]
```

### Weak `тп 3` in 1 target group(s), 1 strong marker(s)

`field_markers.take_profit` strong:

```json
[
  "тп 3:"
]
```

### Weak `тп4` in 1 target group(s), 1 strong marker(s)

`field_markers.take_profit` strong:

```json
[
  "тп4:"
]
```

### Weak `тп 4` in 1 target group(s), 1 strong marker(s)

`field_markers.take_profit` strong:

```json
[
  "тп 4:"
]
```

### Weak `1 тейк` in 2 target group(s), 2 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на 1 тейк"
]
```

`field_markers.take_profit` strong:

```json
[
  "1 тейк:"
]
```

### Weak `2 тейк` in 2 target group(s), 2 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на 2 тейк"
]
```

`field_markers.take_profit` strong:

```json
[
  "2 тейк:"
]
```

### Weak `3 тейк` in 3 target group(s), 5 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на 3 тейк"
]
```

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "убираю 2 и 3 тейк",
  "убираем 2 и 3 тейк",
  "убрать 2 и 3 тейк"
]
```

`field_markers.take_profit` strong:

```json
[
  "3 тейк:"
]
```

### Weak `4 тейк` in 1 target group(s), 1 strong marker(s)

`field_markers.take_profit` strong:

```json
[
  "4 тейк:"
]
```

### Weak `тейк 1` in 2 target group(s), 2 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на тейк 1"
]
```

`field_markers.take_profit` strong:

```json
[
  "тейк 1:"
]
```

### Weak `тейк 2` in 2 target group(s), 2 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на тейк 2"
]
```

`field_markers.take_profit` strong:

```json
[
  "тейк 2:"
]
```

### Weak `тейк 3` in 2 target group(s), 2 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на тейк 3"
]
```

`field_markers.take_profit` strong:

```json
[
  "тейк 3:"
]
```

### Weak `тейк 4` in 1 target group(s), 1 strong marker(s)

`field_markers.take_profit` strong:

```json
[
  "тейк 4:"
]
```

### Weak `первый тейк` in 2 target group(s), 12 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на первый тейк",
  "стоп на первый тейк ставим",
  "стоп ставим на первый тейк",
  "стоп поставить на первый тейк",
  "стоп переносим на первый тейк",
  "стоп перенести на первый тейк",
  "стоп переставляем на первый тейк",
  "стоп переставить на первый тейк",
  "стоп двигаем на первый тейк",
  "стоп передвигаем на первый тейк",
  "стоп поднимаем на первый тейк"
]
```

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "первый тейк убираем"
]
```

### Weak `второй тейк` in 2 target group(s), 11 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на второй тейк",
  "стоп ставим на второй тейк",
  "стоп поставить на второй тейк",
  "стоп переносим на второй тейк",
  "стоп перенести на второй тейк",
  "стоп переставляем на второй тейк",
  "стоп переставить на второй тейк",
  "стоп двигаем на второй тейк",
  "стоп передвигаем на второй тейк",
  "стоп поднимаем на второй тейк"
]
```

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "второй тейк убираем"
]
```

### Weak `третий тейк` in 2 target group(s), 4 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на третий тейк"
]
```

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "убираю второй и третий тейк",
  "убираем второй и третий тейк",
  "третий тейк убираем"
]
```

### Weak `тейк` in 5 target group(s), 102 strong marker(s)

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на 1 тейк",
  "стоп на первый тейк",
  "стоп на первый тейк ставим",
  "стоп ставим на первый тейк",
  "стоп поставить на первый тейк",
  "стоп переносим на первый тейк",
  "стоп перенести на первый тейк",
  "стоп переставляем на первый тейк",
  "стоп переставить на первый тейк",
  "стоп двигаем на первый тейк",
  "стоп передвигаем на первый тейк",
  "стоп поднимаем на первый тейк",
  "стоп на тейк 1",
  "стоп к первому тейку",
  "стоп на 2 тейк",
  "стоп на второй тейк",
  "стоп ставим на второй тейк",
  "стоп поставить на второй тейк",
  "стоп переносим на второй тейк",
  "стоп перенести на второй тейк",
  "стоп переставляем на второй тейк",
  "стоп переставить на второй тейк",
  "стоп двигаем на второй тейк",
  "стоп передвигаем на второй тейк",
  "стоп поднимаем на второй тейк",
  "стоп на тейк 2",
  "стоп к второму тейку",
  "стоп на 3 тейк",
  "стоп на третий тейк",
  "стоп на тейк 3",
  "стоп к третьему тейку"
]
```

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "обновить тейки",
  "можно обновить тейки",
  "тейки обновить",
  "обновляем тейки",
  "обновляю тейки",
  "обновим тейки",
  "тейки обновляем",
  "тейки обновим",
  "новые тейки",
  "ставим новые тейки",
  "изменить тейки",
  "меняем тейки",
  "тейки меняем",
  "поменять тейки",
  "тейки поменять",
  "заменить тейки",
  "заменяем тейки",
  "тейки заменить",
  "тейки заменяем",
  "убираю 2 и 3 тейк",
  "убираем 2 и 3 тейк",
  "убрать 2 и 3 тейк",
  "убираю второй и третий тейк",
  "убираем второй и третий тейк",
  "первый тейк убираем",
  "второй тейк убираем",
  "третий тейк убираем",
  "тейк убираем",
  "тейки убираем",
  "убираем тейк",
  "убираем тейки",
  "убрать тейк",
  "убрать тейки",
  "отменяем тейк",
  "отменяем тейки",
  "отменить тейк",
  "отменить тейки",
  "снимаем тейк",
  "снимаем тейки",
  "снять тейк",
  "снять тейки",
  "добавлю тейк спота",
  "добавляю тейк",
  "добавляем тейк",
  "добавить тейк",
  "добавлю тейк",
  "добавим тейк",
  "добавить еще тейк",
  "добавить ещё тейк",
  "добавляем еще один тейк",
  "добавляем ещё один тейк"
]
```

`target_hint_markers.ALL_LONG` strong:

```json
[
  "тейки по лонгам",
  "тейки по всем лонгам"
]
```

`target_hint_markers.ALL_POSITIONS` strong:

```json
[
  "тейки по всем позициям"
]
```

`field_markers.take_profit` strong:

```json
[
  "тейки",
  "тейки:",
  "тейки :",
  "тейки -",
  "тейки —",
  "тейки по сделке",
  "тейки по позиции",
  "тейк 1:",
  "тейк 2:",
  "тейк 3:",
  "тейк 4:",
  "тейк 5:",
  "1 тейк:",
  "2 тейк:",
  "3 тейк:",
  "4 тейк:",
  "5 тейк:"
]
```

### Weak `тейки` in 4 target group(s), 36 strong marker(s)

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "обновить тейки",
  "можно обновить тейки",
  "тейки обновить",
  "обновляем тейки",
  "обновляю тейки",
  "обновим тейки",
  "тейки обновляем",
  "тейки обновим",
  "новые тейки",
  "ставим новые тейки",
  "изменить тейки",
  "меняем тейки",
  "тейки меняем",
  "поменять тейки",
  "тейки поменять",
  "заменить тейки",
  "заменяем тейки",
  "тейки заменить",
  "тейки заменяем",
  "тейки убираем",
  "убираем тейки",
  "убрать тейки",
  "отменяем тейки",
  "отменить тейки",
  "снимаем тейки",
  "снять тейки"
]
```

`target_hint_markers.ALL_LONG` strong:

```json
[
  "тейки по лонгам",
  "тейки по всем лонгам"
]
```

`target_hint_markers.ALL_POSITIONS` strong:

```json
[
  "тейки по всем позициям"
]
```

`field_markers.take_profit` strong:

```json
[
  "тейки",
  "тейки:",
  "тейки :",
  "тейки -",
  "тейки —",
  "тейки по сделке",
  "тейки по позиции"
]
```

### Weak `цель` in 2 target group(s), 17 strong marker(s)

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "добавить цель",
  "добавляем цель",
  "добавлю цель",
  "убрать цель",
  "убираем цель",
  "обновить цель",
  "меняем цель"
]
```

`field_markers.take_profit` strong:

```json
[
  "цель 1:",
  "цель 2:",
  "цель 3:",
  "цель 4:",
  "цель 5:",
  "1 цель:",
  "2 цель:",
  "3 цель:",
  "4 цель:",
  "5 цель:"
]
```

### Weak `цели` in 4 target group(s), 20 strong marker(s)

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "обновить цели",
  "обновляем цели",
  "цели обновить",
  "цели обновляем",
  "новые цели",
  "ставим новые цели",
  "изменить цели",
  "меняем цели",
  "цели меняем",
  "поменять цели",
  "заменить цели",
  "заменяем цели"
]
```

`target_hint_markers.ALL_LONG` strong:

```json
[
  "цели по лонгам",
  "цели по всем лонгам"
]
```

`target_hint_markers.ALL_POSITIONS` strong:

```json
[
  "цели по всем позициям"
]
```

`field_markers.take_profit` strong:

```json
[
  "цели",
  "цели:",
  "цели :",
  "цели -",
  "цели —"
]
```

### Weak `таргет` in 2 target group(s), 7 strong marker(s)

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "новые таргеты",
  "таргеты обновить",
  "обновить таргеты",
  "обновляем таргеты"
]
```

`field_markers.take_profit` strong:

```json
[
  "таргеты",
  "таргеты:",
  "таргеты -"
]
```

### Weak `таргеты` in 2 target group(s), 7 strong marker(s)

`intent_markers.MODIFY_TARGETS` strong:

```json
[
  "новые таргеты",
  "таргеты обновить",
  "обновить таргеты",
  "обновляем таргеты"
]
```

`field_markers.take_profit` strong:

```json
[
  "таргеты",
  "таргеты:",
  "таргеты -"
]
```

---

## `intent_markers.SL_HIT`

### Weak `стоп` in 6 target group(s), 213 strong marker(s)

`intent_markers.MOVE_STOP_TO_BE` strong:

```json
[
  "стоп в бу",
  "стопы в бу",
  "стоп в б/у",
  "стопы в б/у",
  "стоп в безубыток",
  "стопы в безубыток",
  "стоп в безубытке",
  "стопы в безубытке",
  "стоп переводим в бу",
  "стопы переводим в бу",
  "стоп переводим в б/у",
  "стопы переводим в б/у",
  "стоп переводим в безубыток",
  "стопы переводим в безубыток",
  "переводим стоп в бу",
  "переводим стопы в бу",
  "переводим стоп в б/у",
  "переводим стопы в б/у",
  "переводим стоп в безубыток",
  "переводим стопы в безубыток",
  "перевести стоп в бу",
  "перевести стопы в бу",
  "перевести стоп в б/у",
  "перевести стопы в б/у",
  "перевести стоп в безубыток",
  "перевести стопы в безубыток",
  "переведем стоп в бу",
  "переведем стопы в бу",
  "переведем стоп в безубыток",
  "переведем стопы в безубыток",
  "перенести стоп в бу",
  "перенести стопы в бу",
  "перенести стоп в б/у",
  "перенести стопы в б/у",
  "перенести стоп в безубыток",
  "перенести стопы в безубыток",
  "передвинуть стоп в бу",
  "передвинуть стопы в бу",
  "передвиньте стоп в бу",
  "передвиньте стопы в бу",
  "передвинуть стоп в безубыток",
  "передвинуть стопы в безубыток",
  "передвиньте стоп в безубыток",
  "передвиньте стопы в безубыток",
  "двигать стоп в бу",
  "двигать стопы в бу",
  "двигаем стоп в бу",
  "двигаем стопы в бу",
  "стоп двигаем в бу",
  "стопы двигаем в бу",
  "стоп двигаем в безубыток",
  "стопы двигаем в безубыток",
  "стоп поставим на точку входа",
  "стопы поставим на точку входа",
  "стоп на точку входа",
  "стопы на точку входа",
  "стоп обязательно на точку входа",
  "стопы обязательно на точку входа",
  "поставить стоп на точку входа",
  "поставить стопы на точку входа",
  "поставим стоп на точку входа",
  "поставим стопы на точку входа",
  "ставим стоп на точку входа",
  "ставим стопы на точку входа",
  "стоп на вход",
  "стопы на вход",
  "стоп на цену входа",
  "стопы на цену входа",
  "стоп к цене входа",
  "стопы к цене входа",
  "стоп переносим на вход",
  "стопы переносим на вход",
  "стоп переносим на цену входа",
  "стопы переносим на цену входа",
  "стоп в ноль",
  "стопы в ноль",
  "стоп на ноль",
  "стопы на ноль",
  "перевести стоп в ноль",
  "перевести стопы в ноль",
  "переводим стоп в ноль",
  "переводим стопы в ноль",
  "передвиньте стопы в плюс",
  "передвиньте стоп в плюс",
  "стопы в плюс",
  "стоп в плюс",
  "стоп в небольшой плюс",
  "стопы в небольшой плюс",
  "стоп в минимальный плюс",
  "стопы в минимальный плюс",
  "стоп должен стоять в безубытке",
  "стопы должны стоять в безубытке",
  "стоп должен быть в безубытке",
  "стопы должны быть в безубытке",
  "стоп стоит в безубытке",
  "стопы стоят в безубытке",
  "перевод стопа в бу",
  "перевод стопов в бу",
  "перевод стопа в безубыток",
  "перевод стопов в безубыток",
  "перенос стопа в бу",
  "перенос стопов в бу",
  "перенос стопа в безубыток",
  "перенос стопов в безубыток"
]
```

`intent_markers.MOVE_STOP` strong:

```json
[
  "стоп на 1 тейк",
  "стоп на первый тейк",
  "стоп на первый тейк ставим",
  "стоп ставим на первый тейк",
  "стоп поставить на первый тейк",
  "стоп переносим на первый тейк",
  "стоп перенести на первый тейк",
  "стоп переставляем на первый тейк",
  "стоп переставить на первый тейк",
  "стоп двигаем на первый тейк",
  "стоп передвигаем на первый тейк",
  "стоп поднимаем на первый тейк",
  "стоп на tp1",
  "стоп на тп1",
  "стоп на tp 1",
  "стоп на тп 1",
  "стоп на тейк 1",
  "стоп к первому тейку",
  "стоп к tp1",
  "стоп к тп1",
  "стоп на 2 тейк",
  "стоп на второй тейк",
  "стоп ставим на второй тейк",
  "стоп поставить на второй тейк",
  "стоп переносим на второй тейк",
  "стоп перенести на второй тейк",
  "стоп переставляем на второй тейк",
  "стоп переставить на второй тейк",
  "стоп двигаем на второй тейк",
  "стоп передвигаем на второй тейк",
  "стоп поднимаем на второй тейк",
  "стоп на tp2",
  "стоп на тп2",
  "стоп на tp 2",
  "стоп на тп 2",
  "стоп на тейк 2",
  "стоп к второму тейку",
  "стоп к tp2",
  "стоп к тп2",
  "стоп на 3 тейк",
  "стоп на третий тейк",
  "стоп на tp3",
  "стоп на тп3",
  "стоп на тейк 3",
  "стоп к третьему тейку",
  "переставить стоп",
  "переставляем стоп",
  "переставьте стоп",
  "стоп переставляем",
  "стоп переставить",
  "стоп переставьте",
  "перенести стоп",
  "переносим стоп",
  "переношу стоп",
  "перенесите стоп",
  "стоп переносим",
  "стоп перенести",
  "стоп переношу",
  "перенос стопа",
  "перенос стопов",
  "сдвигаем стоп",
  "сдвинуть стоп",
  "сдвиньте стоп",
  "стоп сдвигаем",
  "двигаем стоп",
  "двигать стоп",
  "двинуть стоп",
  "стоп двигаем",
  "стоп двигаем на",
  "стоп передвигаем",
  "передвинуть стоп",
  "передвиньте стоп",
  "поднять стоп",
  "поднимаем стоп",
  "поднимите стоп",
  "стоп поднимаем",
  "опустить стоп",
  "опускаем стоп",
  "стоп опускаем",
  "новый стоп",
  "ставим новый стоп",
  "поставить новый стоп",
  "стоп новый",
  "стоп поставьте на",
  "стоп поставим на",
  "стоп поставить на",
  "стоп ставим на",
  "стоп ставлю на",
  "стоп переношу на",
  "стоп переносим на",
  "стоп переставляем на"
]
```

`intent_markers.EXIT_BE` strong:

```json
[
  "стоп в бу сработал",
  "стоп в б/у сработал",
  "стоп в безубытке сработал",
  "сработал стоп в бу",
  "сработал стоп в б/у",
  "сработал стоп в безубытке"
]
```

`target_hint_markers.ALL_LONG` strong:

```json
[
  "стопы по лонгам",
  "стопы по всем лонгам",
  "стопы всем лонгам",
  "стопы на все лонги",
  "по всем лонгам стоп",
  "по всем лонгам стопы",
  "стоп по всем лонгам",
  "стоп всем лонгам"
]
```

`target_hint_markers.ALL_POSITIONS` strong:

```json
[
  "стопы по всем позициям",
  "стопы всем позициям",
  "по всем позициям стопы"
]
```

`field_markers.stop_loss` strong:

```json
[
  "стоп:"
]
```

### Weak `sl` in 1 target group(s), 1 strong marker(s)

`field_markers.stop_loss` strong:

```json
[
  "sl:"
]
```

### Weak `выбило` in 1 target group(s), 5 strong marker(s)

`intent_markers.EXIT_BE` strong:

```json
[
  "выбило по бу",
  "выбило по б/у",
  "выбило в бу",
  "выбило в б/у",
  "выбило в безубыток"
]
```

---

## `intent_markers.EXIT_BE`

### Weak `бу` in 3 target group(s), 36 strong marker(s)

`intent_markers.MOVE_STOP_TO_BE` strong:

```json
[
  "стоп в бу",
  "стопы в бу",
  "стоп переводим в бу",
  "стопы переводим в бу",
  "переводим стоп в бу",
  "переводим стопы в бу",
  "перевести стоп в бу",
  "перевести стопы в бу",
  "переведем стоп в бу",
  "переведем стопы в бу",
  "перенести стоп в бу",
  "перенести стопы в бу",
  "передвинуть стоп в бу",
  "передвинуть стопы в бу",
  "передвиньте стоп в бу",
  "передвиньте стопы в бу",
  "в бу двигать",
  "двигать стоп в бу",
  "двигать стопы в бу",
  "двигаем стоп в бу",
  "двигаем стопы в бу",
  "стоп двигаем в бу",
  "стопы двигаем в бу",
  "перевод в бу",
  "перевод стопа в бу",
  "перевод стопов в бу",
  "перенос стопа в бу",
  "перенос стопов в бу"
]
```

`intent_markers.MODIFY_ENTRY` strong:

```json
[
  "попробуем шорт с текущих",
  "попробуем лонг с текущих",
  "пробуем шорт с текущих",
  "пробуем лонг с текущих"
]
```

`modify_entry_mode_markers.MARKET_NOW` strong:

```json
[
  "попробуем шорт с текущих",
  "попробуем лонг с текущих",
  "пробуем шорт с текущих",
  "пробуем лонг с текущих"
]
```

### Weak `б/у` in 1 target group(s), 11 strong marker(s)

`intent_markers.MOVE_STOP_TO_BE` strong:

```json
[
  "стоп в б/у",
  "стопы в б/у",
  "стоп переводим в б/у",
  "стопы переводим в б/у",
  "переводим стоп в б/у",
  "переводим стопы в б/у",
  "перевести стоп в б/у",
  "перевести стопы в б/у",
  "перенести стоп в б/у",
  "перенести стопы в б/у",
  "перевод в б/у"
]
```

### Weak `безубыток` in 1 target group(s), 23 strong marker(s)

`intent_markers.MOVE_STOP_TO_BE` strong:

```json
[
  "стоп в безубыток",
  "стопы в безубыток",
  "стоп переводим в безубыток",
  "стопы переводим в безубыток",
  "переводим стоп в безубыток",
  "переводим стопы в безубыток",
  "перевести стоп в безубыток",
  "перевести стопы в безубыток",
  "переведем стоп в безубыток",
  "переведем стопы в безубыток",
  "перенести стоп в безубыток",
  "перенести стопы в безубыток",
  "передвинуть стоп в безубыток",
  "передвинуть стопы в безубыток",
  "передвиньте стоп в безубыток",
  "передвиньте стопы в безубыток",
  "стоп двигаем в безубыток",
  "стопы двигаем в безубыток",
  "перевод в безубыток",
  "перевод стопа в безубыток",
  "перевод стопов в безубыток",
  "перенос стопа в безубыток",
  "перенос стопов в безубыток"
]
```

### Weak `безубытке` in 1 target group(s), 11 strong marker(s)

`intent_markers.MOVE_STOP_TO_BE` strong:

```json
[
  "стоп в безубытке",
  "стопы в безубытке",
  "стоп должен стоять в безубытке",
  "стопы должны стоять в безубытке",
  "стоп должен быть в безубытке",
  "стопы должны быть в безубытке",
  "стоп стоит в безубытке",
  "стопы стоят в безубытке",
  "стоять в безубытке",
  "должен стоять в безубытке",
  "должны стоять в безубытке"
]
```

### Weak `в ноль` in 1 target group(s), 6 strong marker(s)

`intent_markers.MOVE_STOP_TO_BE` strong:

```json
[
  "стоп в ноль",
  "стопы в ноль",
  "перевести стоп в ноль",
  "перевести стопы в ноль",
  "переводим стоп в ноль",
  "переводим стопы в ноль"
]
```

---

## `modify_entry_mode_markers.MARKET_NOW`

### Weak `по рынку` in 4 target group(s), 26 strong marker(s)

`intent_markers.CLOSE_FULL` strong:

```json
[
  "закрываю по рынку",
  "закрываем по рынку",
  "закрыть по рынку",
  "закрою по рынку",
  "закрываю все по рынку",
  "закрытие по рынку",
  "выход по рынку"
]
```

`intent_markers.MODIFY_ENTRY` strong:

```json
[
  "вход теперь по рынку",
  "вход по рынку",
  "входим по рынку",
  "зайду по рынку",
  "заходим по рынку",
  "беру по рынку",
  "берем по рынку",
  "берём по рынку",
  "зайдем по рынку",
  "зайдём по рынку",
  "открываю по рынку",
  "открываем по рынку",
  "открыть по рынку",
  "по рынку"
]
```

`target_hint_markers.ALL_LONG` strong:

```json
[
  "закрыть лонги по рынку"
]
```

`target_hint_markers.ALL_POSITIONS` strong:

```json
[
  "закрываю все по рынку",
  "закрываем все по рынку",
  "закрыть все по рынку",
  "закрою все по рынку"
]
```

---

## `entry_type_markers.LIMIT`

### Weak `лимиткой` in 1 target group(s), 1 strong marker(s)

`field_markers.entry` strong:

```json
[
  "вход лимиткой"
]
```

---

## `target_hint_markers.ALL_LONG`

### Weak `лонги` in 1 target group(s), 7 strong marker(s)

`intent_markers.CLOSE_FULL` strong:

```json
[
  "зафиксировать все лонги",
  "закрыть лонги по текущим",
  "закрываю все лонги",
  "закрываем все лонги",
  "закрыть все лонги",
  "лонги закрыть",
  "лонги закрываем"
]
```

---

## `target_hint_markers.ALL_SHORT`

### Weak `шорты` in 1 target group(s), 7 strong marker(s)

`intent_markers.CLOSE_FULL` strong:

```json
[
  "зафиксировать все шорты",
  "закрыть шорты по текущим",
  "закрываю все шорты",
  "закрываем все шорты",
  "закрыть все шорты",
  "шорты закрыть",
  "шорты закрываем"
]
```

### Weak `short` in 1 target group(s), 1 strong marker(s)

`side_markers.SHORT` strong:

```json
[
  "short"
]
```

---

## `field_markers.symbol`

### Weak `.p` in 1 target group(s), 1 strong marker(s)

`target_hint_markers.symbol` strong:

```json
[
  ".p"
]
```

---

## `field_markers.entry`

### Weak `усреднение` in 6 target group(s), 35 strong marker(s)

`intent_markers.CANCEL_PENDING` strong:

```json
[
  "лимитку на усреднение убираем",
  "лимитку на усреднение обязательно убираем",
  "лимитку на усреднение можно убрать",
  "лимитки на усреднение убираем",
  "убираем лимитку на усреднение",
  "убрать лимитку на усреднение",
  "отменяем лимитку на усреднение",
  "отменить лимитку на усреднение",
  "снимаем лимитку на усреднение",
  "снять лимитку на усреднение"
]
```

`intent_markers.MODIFY_ENTRY` strong:

```json
[
  "усреднение переносим",
  "переносим усреднение",
  "усреднение меняем",
  "меняем усреднение",
  "лимитку на усреднение переносим",
  "переносим лимитку на усреднение"
]
```

`intent_markers.ADD_ENTRY` strong:

```json
[
  "добавляю усреднение",
  "добавляем усреднение",
  "добавить усреднение",
  "добавлю усреднение",
  "добавим усреднение",
  "ставим усреднение",
  "поставим усреднение",
  "поставить усреднение",
  "добавляю усреднение на",
  "добавляем усреднение на",
  "добавить усреднение на"
]
```

`intent_markers.ENTRY_FILLED` strong:

```json
[
  "усреднение взято",
  "взяли усреднение",
  "усреднение исполнилось",
  "усреднение исполнено",
  "усреднение сработало"
]
```

`modify_entry_mode_markers.UPDATE_PRICE` strong:

```json
[
  "усреднение переносим"
]
```

`entry_selector_markers.AVERAGING` strong:

```json
[
  "усреднение",
  "лимитка на усреднение"
]
```

---
